To use the Stellaris, comment the line

ASSERT(c != 0)

in the Hardware_serial.cpp file in the method write at hardware/lm4f/cores/lm4f




Modificar los archivos:

Ejecutar el archivo rename.sh o manualmente las cosas:

.bashrc
catkin/src/finder/launch/machines_test.launch
rosbuild/.rosinstall
rosbuild/.setup.bash
rosbuild/.setup.sh
